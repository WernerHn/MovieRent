<%= form_with(model: line_movie, local: true) do |form| %>
  <% if line_movie.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(line_movie.errors.count, "error") %> prohibited this line_movie from being saved:</h2>

      <ul>
      <% line_movie.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :movie_id %>
    <%= form.number_field :movie_id %>
  </div>

  <div class="field">
    <%= form.label :cart_id %>
    <%= form.number_field :cart_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
